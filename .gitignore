# Per-run artifacts (idea pools, hat analyses, final reports).
# Local working state — not shared; see CLAUDE.md.
runs/

# Local Russian mirror of maintainer docs (canonical CLAUDE.md is English).
CLAUDE.ru.md

# Environment & local overrides
.env
.env.*
!.env.example

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Python (scripts / tooling if added)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (CLI / tooling if added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Build / packaging (future tooling)
dist/
build/
*.egg-info/

# Merge / backup cruft
*.orig
*.bak
*.tmp

# Archives (accidental drops in repo root)
*.zip
*.tar.gz
